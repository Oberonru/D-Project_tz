# 🚀 D-Project_tz  
**Разработчик C# Unity 3D (VR)**  

## 📋 Пример тестового задания  
Предоставлен код проекта с использованием **синглтона**, отвечающего за инициализацию игрока в компьютерной игре.  

### 🎮 Атрибуты игрока  
- ❤️ Здоровье  
- 💎 Количество жизней  
- 🏷️ Никнейм  
- 📊 Таблица навыков  

### 🛠️ Экипировка игрока  
- 🔫 Винтовка с патронами  
- 🪂 Парашют  
- 🚀 Ракетный ранец с произвольным количеством зарядов  

---

## 🧩 Задачи
Необходимо переделать код так, чтобы использовать **Dependency Injection**, и выполнить следующие пункты:

1️⃣ Создать интерфейс для класса `Player`.  
2️⃣ Реализовать класс `Player`, используя интерфейс.  
3️⃣ Создать контейнер **IoC** и зарегистрировать класс `Player` в контейнере.  
4️⃣ Использовать контейнер для получения экземпляра класса `Player` в другом коде.  
5️⃣ Объяснить, какие изменения были внесены и как они влияют на работу кода.  
6️⃣ Продемонстрировать использование нового кода в другом классе, например, в `Main`.  
7️⃣ Добавить комментарии к коду, объясняющие его работу.  
8️⃣ Соблюсти принципы **SOLID** при переделке кода.  
9️⃣ Убедиться, что новый код позволяет легко изменять значения атрибутов игрока (здоровье, количество патронов и т. д.) из разных частей программы.  
🔟 Обеспечить безопасность использования синглтона: предотвратить возможность создания нескольких экземпляров класса `Player` и гарантировать уникальность данных игрока.  
1️⃣1️⃣ Добавить в класс `Player` параметры экипировки: винтовку с патронами, парашют и ракетный ранец.  
1️⃣2️⃣ Сделать так, чтобы параметры элементов экипировки можно было задавать в **рантайме** через Dependency Injection.  

---

## ✅ Требования
- Код должен быть чистым и соответствовать принципам **SOLID**.  
- Все изменения должны быть задокументированы комментариями.  
- Демонстрация работы должна быть показана в отдельном примере (например, класс `Main`).  

---

## 📂 Итог
В результате выполнения задания должен получиться проект, где:  
- Игрок (`Player`) создаётся и управляется через **IoC контейнер**.  
- Атрибуты и экипировка задаются через **Dependency Injection**.  
- Код легко расширяем, поддерживаем и безопасен в использовании.  

---

## ✅ Сделано

- 🧍 Реализован базовый класс для игрока **PlayerInstance**
- 🔗 Создан интерфейс **IPlayerInstance**, работа идёт через него
- ⚙️ Реализована система **конфигов** — можно менять параметры игрока, оружия, предметов и настройку горячих клавиш без изменения кода
- 🧩 Разделены компоненты (**Health, Inventory, Equipment, Weapon, PlayerCombat**)  
  - Оружие вынесено отдельным компонентом для дальнейшей модификации  
  - Инвентарь — это всё, что есть у игрока, экипировка — то, что на нём надето
- 🚀 Создан **PlayerSpawner**, использующий **PlayerFactory** для создания персонажа и инициализации зависимостей  
  - После спавна экземпляр игрока регистрируется в DI‑контейнере и доступен для инжекции в любых зависимостях
- 🧪 Созданы тесты в отдельной сборке
- 🛠️ Реализована система **DevTools** в Unity для визуального тестирования реализации (используется инжекция `IPlayerInstance`)

